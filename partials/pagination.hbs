<nav class="pagination">
  {{#if prev}}
  <a rel="next" class="posts newer btn small" href="{{page_url prev}}#open">&larr; Nuevos Posts</a>
  {{/if}}
  <span class="posts index">Página {{page}} de {{pages}}</span>
  {{#if next}}
  <a rel="prev" class="posts older btn small" href="{{page_url next}}#open">Anteriores Posts &rarr;</a>
  {{/if}}
</nav>
